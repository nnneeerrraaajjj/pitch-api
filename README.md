# pitch-api
Pitch API for hackathon

To setup app
First create python virtual env
```python3 -m venv env```

Activate the env
```source ./env/bin/activate```

Then install requirements
```pip3 install -r requirements.txt```

Run the server
```uvicorn main:app --host 0.0.0.0 --port 8000```
